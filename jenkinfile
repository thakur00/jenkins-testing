pipeline {
    agent any

    stages {
        stage('Install Dependencies and Restart PM2') {
            steps {
                script {
                    sh 'cd /Users/apple/dedeeededed/jenkins-testing && ls && git pull'
                    // Install npm packages
                    sh 'whoami'
                    //
                    sh 'npm i'
                    // Restart all PM2 processes
                    sh 'pm2 restart 0'
                }
            }
        }
    }
}
